<!DOCTYPE html>
<html>
<head>
	<title>New VS</title>
</head>
<body>
	<div> <h1> 
		Create a new vendor sweet: 
	</h1> </div>
	<div> 
		<%= form_for :vendor_sweet do |f| %>
			<%= f.label :price %>
			<%= f.number_field :price %>
			<%= f.collection_select :vendor_id, Vendor.all, :id, :name %>
			<%= f.collection_select :sweet_id, Sweet.all, :id, :name %>
			<%= f.submit "Make this vendor sweet!" %>
		<% end %>
	</div>
</body>
</html>